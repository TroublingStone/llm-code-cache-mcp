[project]
name = "llm-code-cache-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
    "ruff>=0.15.12",
    "tree-sitter>=0.25.2",
    "tree-sitter-python>=0.25.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src/llm_code_cache"]
testpaths = ["tests"]
